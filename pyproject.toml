[project]
name = "mapf"
version = "0.1.0"
description = "Prototipo de Multi-Agent Path Finding (MAPF) sobre un grafo con nodos prima/sombra."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.7",
    "networkx>=3.6.1",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/domain", "src/data_loaders", "src/algorithms"]

[tool.pytest.ini_options]
testpaths = ["tests"]
